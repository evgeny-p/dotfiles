---
- name: Deploy config.fish
  ansible.builtin.template:
    src: config.fish.j2
    dest: "{{ fish_config_dir }}/config.fish"
    mode: "0644"
    backup: true

- name: Deploy conf.d snippets
  ansible.builtin.template:
    src: "conf.d/{{ item }}.j2"
    dest: "{{ fish_config_dir }}/conf.d/{{ item }}"
    mode: "0644"
  loop:
    - 00-environment.fish
    - 10-abbreviations.fish
